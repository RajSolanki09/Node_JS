<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <!-- Remix Icon CDN -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />

    <!-- Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.3/dist/tailwind.min.css" rel="stylesheet">

    <!-- Tailwind CSS Script for Custom Configuration -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- External CSS -->
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between fixed top-0 left-0 px-5 py-3">
        <h3 class="text-xl">Scatch</h3>
        <% var loggedin = typeof(loggedin) === "undefined" ? true : loggedin %>
        <% if(loggedin) { %>
            <div class="flex gap-5">
                <a href="">Shop</a>
                <a href="/cart">Cart</a>
                <a href="">My Account</a>
                <a class="text-red-500" href="/logout">Logout</a>
            </div>
        <% } %>
    </nav>
</body>

</html>
